<div class="fq-actions"><a class="btn btn-sm btn-outline-primary" href="{{ route('preinvoice.draft.finance',$o->uuid) }}">بررسی و تأیید</a>@if($o->can_finance_edit)<a class="btn btn-sm btn-warning" href="{{ route('preinvoice.draft.finance.edit',$o->uuid) }}">✏️ ویرایش مالی</a>@endif<form method="POST" action="{{ route('preinvoice.draft.finalize',$o->uuid) }}" data-guard-submit>@csrf<button class="btn btn-sm btn-success" data-disable-on-expire {{ $isExpired ? 'disabled' : '' }}>تأیید مالی و ارسال به جمع‌آوری</button></form><div class="dropdown"><button class="btn btn-sm btn-outline-secondary dropdown-toggle" data-bs-toggle="dropdown">بیشتر</button><ul class="dropdown-menu"><li><a class="dropdown-item" href="{{ route('preinvoice.print',$o->uuid) }}">چاپ</a></li><li><button class="dropdown-item text-warning" data-bs-toggle="modal" data-bs-target="#returnModal{{ $o->id }}" data-disable-on-expire {{ $isExpired ? 'disabled' : '' }}>ارجاع به فروشنده</button></li><li><button class="dropdown-item text-danger" data-bs-toggle="modal" data-bs-target="#cancelModal{{ $o->id }}">لغو پیش‌فاکتور</button></li></ul></div></div>
