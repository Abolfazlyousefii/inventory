@php($seconds=$o->getAttribute('finance_seconds_remaining')) @php($label=$o->getAttribute('finance_reservation_label')) @php($expiresIso=$o->stock_frozen_until?->toIso8601String())
<div class="reservation-box" data-reservation-timer data-expires-at="{{ $expiresIso }}" data-total-seconds="{{ $seconds ?? 0 }}" data-label="{{ $label }}"><span class="badge {{ $o->getAttribute('finance_reservation_expired') ? 'text-bg-danger' : 'text-bg-success' }} reservation-status">{{ $label }}</span><div class="small fq-muted">زمان باقی‌مانده رزرو:</div><div class="reservation-countdown">{{ $expiresIso ? '—' : $label }}</div>@if($expiresIso)<div class="timer-progress"><span style="width:100%"></span></div>@endif</div>
