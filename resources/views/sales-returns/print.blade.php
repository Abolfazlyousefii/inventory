@php
    $customerName = $document->customer ? (trim(($document->customer->first_name ?? '').' '.($document->customer->last_name ?? '')) ?: $document->customer->mobile) : '—';
    $reference = $document->isInternal() ? ($document->invoice?->uuid ?: '—') : ($document->external_invoice_number ?: '—');
@endphp
<!doctype html><html lang="fa" dir="rtl"><head><meta charset="utf-8"><title>چاپ برگشت از فروش {{ $document->document_number }}</title><style>body{font-family:Tahoma,Arial,sans-serif;color:#111827;background:#f1f5f9;margin:0}.page{width:210mm;min-height:297mm;margin:12px auto;background:#fff;padding:14mm;box-sizing:border-box}.print-actions{text-align:left;margin:12px auto;width:210mm}.btn{border:1px solid #334155;border-radius:8px;padding:8px 14px;background:#fff;cursor:pointer}.head{border-bottom:2px solid #1e3a5f;padding-bottom:12px;display:flex;justify-content:space-between;align-items:center}.title{font-size:24px;font-weight:900;color:#1e3a5f}.logo{width:64px;height:64px;object-fit:contain}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin:14px 0}.box{border:1px solid #e2e8f0;border-radius:8px;padding:8px;font-size:12px}.box small{display:block;color:#64748b;margin-bottom:4px}table{width:100%;border-collapse:collapse;font-size:12px;margin-top:12px}th,td{border:1px solid #cbd5e1;padding:7px;text-align:right}th{background:#f8fafc}.sum{text-align:left;font-weight:900;margin-top:12px}.signatures{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-top:28px}.sig{height:90px;border:1px dashed #94a3b8;border-radius:8px;padding:8px;color:#64748b}@page{size:A4;margin:8mm}@media print{body{background:#fff}.page{margin:0;width:auto;min-height:auto}.print-actions{display:none}}</style></head><body><div class="print-actions"><button class="btn" onclick="window.print()">چاپ</button><a class="btn" href="{{ route('sales-returns.show',$document) }}">بازگشت</a></div><main class="page"><div class="head"><div><div class="title">برگشت از فروش</div><div>شماره سند: {{ $document->document_number }} | وضعیت: {{ $document->statusLabel() }}</div></div><img class="logo" src="{{ asset('logo.png') }}" alt="logo"></div><div class="grid"><div class="box"><small>تاریخ</small>{{ optional($document->created_at)->format('Y/m/d H:i') }}</div><div class="box"><small>مشتری</small>{{ $customerName }}</div><div class="box"><small>فاکتور مرجع</small>{{ $reference }}</div><div class="box"><small>نوع برگشت</small>{{ $document->sourceTypeLabel() }}</div><div class="box"><small>علت برگشت</small>{{ $document->return_reason ?: '—' }}</div><div class="box"><small>ثبت‌کننده</small>{{ $document->creator?->name ?: '—' }}</div></div><table><thead><tr><th>#</th><th>کالا</th><th>تنوع</th><th>تعداد</th><th>وضعیت</th><th>انبار مقصد</th><th>مبلغ واحد</th><th>مبلغ کل</th></tr></thead><tbody>@foreach($document->items as $item)<tr><td>{{ $loop->iteration }}</td><td>{{ $item->product_name_snapshot ?: $item->product?->name ?: '—' }}</td><td>{{ $item->variant_name_snapshot ?: $item->sku_snapshot ?: '—' }}</td><td>{{ number_format($item->return_quantity) }}</td><td>{{ $item->isDamaged() ? 'معیوب' : 'سالم' }}</td><td>{{ $item->destinationWarehouse?->name ?: '—' }}</td><td>{{ number_format($item->refund_unit_price) }}</td><td>{{ number_format($item->refund_amount) }}</td></tr>@endforeach</tbody></table><div class="sum">جمع مبلغ بستانکاری: {{ number_format($document->refund_total) }} ریال</div><div class="box" style="margin-top:12px"><small>توضیحات</small>{{ $document->description ?: '—' }}</div><div class="signatures"><div class="sig">امضای ثبت‌کننده</div><div class="sig">امضای مالی</div><div class="sig">امضای انبار</div></div></main></body></html>
